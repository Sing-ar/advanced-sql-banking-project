TRUNCATE TABLE bronze.accounts;
BULK INSERT bronze.accounts 
FROM 'E:\ForDataset\archive\banking_dataset_kaggle\data\csv\accounts.csv'
WITH (
	FIRSTROW = 2,
	FIELDTERMINATOR = ',',
	ROWTERMINATOR = '0x0a',
	TABLOCK

);

TRUNCATE TABLE bronze.branches;
BULK INSERT bronze.branches
FROM 'E:\ForDataset\archive\banking_dataset_kaggle\data\csv\branches.csv'
WITH (
	FIRSTROW = 2,
	FIELDTERMINATOR = ',',
	ROWTERMINATOR = '0x0a',
	TABLOCK

);

TRUNCATE TABLE bronze.cards;
BULK INSERT bronze.cards
FROM 'E:\ForDataset\archive\banking_dataset_kaggle\data\csv\cards.csv'
WITH (
	FIRSTROW = 2,
	FIELDTERMINATOR = ',',
	ROWTERMINATOR = '0x0a',
	TABLOCK

);

TRUNCATE TABLE bronze.customers;
BULK INSERT bronze.customers
FROM 'E:\ForDataset\archive\banking_dataset_kaggle\data\csv\customers.csv'
WITH (
	FIRSTROW = 2,
	FIELDTERMINATOR = ',',
	ROWTERMINATOR = '0x0a',
	TABLOCK

);

TRUNCATE TABLE bronze.loans;
BULK INSERT bronze.loans
FROM 'E:\ForDataset\archive\banking_dataset_kaggle\data\csv\loans.csv'
WITH (
	FIRSTROW = 2,
	FIELDTERMINATOR = ',',
	ROWTERMINATOR = '0x0a',
	TABLOCK

);

TRUNCATE TABLE bronze.merchants;
BULK INSERT bronze.merchants
FROM 'E:\ForDataset\archive\banking_dataset_kaggle\data\csv\merchants.csv'
WITH (
	FIRSTROW = 2,
	FIELDTERMINATOR = ',',
	ROWTERMINATOR = '0x0a',
	TABLOCK

);
